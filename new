<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport"
        content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>Modern iOS Home Screen</title>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet">

    <style>
        html,
        body {
            margin: 0;
            padding: 0;
            min-height: 100vh;
            font-family: 'Inter', sans-serif;
            color: white;
            overflow-x: hidden;
        }

        body {
            background-image: url('./images/ios.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }

        .overlay {
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            z-index: 0;
        }

        .container {
            position: relative;
            z-index: 10;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            padding-top: env(safe-area-inset-top);
        }
        .search-bar {
            width: 90%;
            max-width: 500px;
            margin: 1rem auto;
            position: relative;
        }

        /* Clock */
        .clock-widget {
            text-align: center;
            padding: 1rem 0;
        }

        .clock-widget h1 {
            font-size: clamp(3rem, 10vw, 6rem);
            font-weight: 800;
            margin: 0;
        }

        .clock-sub {
            margin-top: .5rem;
            font-size: clamp(0.7rem, 2vw, 0.9rem);
            opacity: 0.9;
        }

        /* App grid */
        .app-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(76px, 1fr));
            gap: clamp(0.75rem, 3vw, 1.2rem);
            padding: 0 1rem 150px;
        }

        .app-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.4rem;
        }

        .app-icon {
            width: clamp(60px, 14vw, 72px);
            height: clamp(60px, 14vw, 72px);
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 6px 14px rgba(0, 0, 0, .35);
            transition: transform .2s ease;
        }

        .app-icon:hover {
            transform: scale(1.06);
        }

        .app-label {
            font-size: clamp(0.7rem, 2vw, 0.9rem);
            text-align: center;
            opacity: 0.9;
        }

        /* Dock */
        .dock-container {
            position: fixed;
            left: 0;
            right: 0;
            bottom: env(safe-area-inset-bottom, 10px);
            display: flex;
            justify-content: center;
            z-index: 50;
        }

        .dock {
            display: flex;
            gap: clamp(1rem, 4vw, 1.8rem);
            padding: .75rem clamp(1.2rem, 5vw, 2rem);
            background: rgba(255, 255, 255, .12);
            backdrop-filter: blur(20px);
            border-radius: 28px;
        }

        .dock-icon {
            width: clamp(52px, 13vw, 64px);
            height: clamp(52px, 13vw, 64px);
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .material-symbols-outlined {
            font-variation-settings: 'FILL' 1;
        }
    </style>
</head>

<body>
    <div class="overlay"></div>

    <div class="container">
        <!-- Search bar -->
        <div class="search-bar">
        </div>
        <!-- Search bar -->
        <div class="search-bar">
        </div>

        <div class="clock-widget">
            <h1>10:42</h1>
            <div class="clock-sub">Mon, Oct 24 • 72° Clear</div>
        </div>

        <div class="app-grid">
            <div class="app-item">
                <div class="app-icon" style="background:linear-gradient(#3b82f6,#1e40af)">
                    <span class="material-symbols-outlined">chat_bubble</span>
                </div>
                <span class="app-label">Social</span>
            </div>

            <div class="app-item">
                <div class="app-icon" style="background:linear-gradient(#38bdf8,#1d4ed8)">
                    <span class="material-symbols-outlined">mail</span>
                </div>
                <span class="app-label">Mail</span>
            </div>

            <div class="app-item">
                <div class="app-icon" style="background:linear-gradient(#22c55e,#065f46)">
                    <span class="material-symbols-outlined">map</span>
                </div>
                <span class="app-label">Maps</span>
            </div>

            <div class="app-item">
                <div class="app-icon" style="background:linear-gradient(#e5e7eb,#9ca3af)">
                    <span class="material-symbols-outlined" style="color:#111">photo_camera</span>
                </div>
                <span class="app-label">Photos</span>
            </div>
        </div>

        <div class="dock-container">
            <div class="dock">
                <div class="dock-icon" style="background:linear-gradient(#22c55e,#065f46)">
                    <span class="material-symbols-outlined">call</span>
                </div>
                <div class="dock-icon" style="background:linear-gradient(#3b82f6,#1e40af)">
                    <span class="material-symbols-outlined">public</span>
                </div>
                <div class="dock-icon" style="background:linear-gradient(#22c55e,#065f46)">
                    <span class="material-symbols-outlined">chat</span>
                </div>
                <div class="dock-icon" style="background:linear-gradient(#e5e7eb,#9ca3af)">
                    <span class="material-symbols-outlined" style="color:#111">photo_camera</span>
                </div>
            </div>
        </div>

    </div>
</body>

</html>